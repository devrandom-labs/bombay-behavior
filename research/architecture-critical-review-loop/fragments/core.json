{
  "obligations": [
    {"id":"CORE-BEHAVIOR","category":"core","subject":"Behavior pure fold","question":"Prove one typed event produces the explicit result without hidden runtime effects.","status":"resolved","decision":"Retain as-is, evidence verified: the fold is an actor-model law (Agha 1986 become) realized as Bombay derivation — Behavior::step folds one typed Self::Event into Result<Actions,_> with no effect execution; init is the fold's explicit initial observation.","evidence":["crates/behavior/src/behavior.rs:419 pub trait Behavior with associated types Addr/Msg/Event/Sends/Ph/Error/Birth and only init/step methods, no effect-execution seat","crates/behavior/src/behavior.rs:428-430 init(&mut self) and step(&mut self, event: Self::Event) both return impl Future<Output = BehaviorActed<Self>> + Send","crates/behavior/src/behavior.rs:352 Acted = Result<Actions<A, Ph, Sends, Birth>, E>; step can only return the declared Actions algebra or the declared Error","crates/behavior/src/behavior.rs:412 compile_fail doctest proves B::Effect escape seat is absent","crates/behavior/src/behavior.rs:537-543 Base::step delegates to State::handle and Base::init returns Ok(Actions::cont()), no hidden effects"],"validation":["cargo nextest run -p bombay-behavior -> 31 tests run: 31 passed, 0 skipped (behavior fold and composition suite)","cargo test -p bombay-behavior --doc -> 1 passed: crates/behavior/src/behavior.rs - algebra::Behavior (line 412) - compile fail ... ok","rg -n '\\bdyn\\s+[A-Za-z_]|\\bAny\\b|\\bTypeId\\b|\\bunsafe\\b' crates/behavior/src -> 0 matches, so no hidden dynamic effect channel exists"]},
    {"id":"CORE-ACTIONS","category":"core","subject":"Actions boundary","question":"Prove sends, staged creations, and next behavior/termination remain complete and explicit.","status":"resolved","decision":"Retain as-is, evidence verified: Bombay derivation of the Agha triple (send/create/become) — Actions holds exactly sends: Sends, creates: Vec<Create<A, Birth::Child>>, become_: Become<A,Ph>; interpreter ordering (creates before sends, per-lane order preserved) is documented policy on the type.","evidence":["crates/behavior/src/behavior.rs:320 pub struct Actions<A: Address, Ph, Sends, Birth: BirthMode> with public fields sends, creates, become_ — complete and explicit, no other effect lane","crates/behavior/src/behavior.rs:310-318 doc comment states the interpreter ordering policy: installs every fresh actor in creates before interpreting any delivery or ServiceSends request; creation order is vector order; SendProduct lanes keep their own order","crates/behavior/src/behavior.rs:118 pub trait SendAlgebra: Sized { fn empty(); fn append(&mut self, other: Self) } — the monoid used by the driver to accumulate sends","crates/behavior/src/behavior.rs:556-608 run() driver accumulates via sends.append(actions.sends) and creates.extend(actions.creates), exhausting become_ on Step"],"validation":["cargo nextest run -p bombay-behavior-testkit --test driver_accumulation -> 6 tests run: 6 passed, including send_algebra_is_a_monoid and driver_accumulates_supervising_send_products_losslessly","cargo nextest run -p bombay-behavior -> 31 passed including actions_expose_the_typed_actor_transition_effects and ordinary_and_service_send_algebras_have_disjoint_static_dispatch","crates/behavior-testkit/tests/driver_accumulation.rs:143-194 proptest send_algebra_is_a_monoid verifies empty two-sided identity and append associativity for Vec and SendProduct"]},
    {"id":"CORE-INITIALIZATION","category":"core","subject":"initialization contract","question":"Prove initialization effects compose and precede mailbox events in documented order.","status":"resolved","decision":"Retain as-is, evidence verified: init-before-mailbox is a Bombay policy enforced structurally by the driver (init awaited before any recv) and compositionally by wrappers (inner init first, then own lane effects appended); double-init/step-before-init are documented direct-misuse boundaries, unreachable through the driver.","evidence":["crates/behavior/src/behavior.rs:566-580 run() awaits behavior.init() and folds its effects before entering the mailbox loop — init effects precede every mailbox event","crates/behavior/src/deadlined.rs:117-133 At::init calls inner.init() first, then appends its own ScheduleAt only when not stopping","crates/behavior/src/watching.rs:116-122 Watching::init wraps inner actions and appends ServiceSends::one(ObservePeer) — inner effects precede own lane","crates/behavior/src/receive_timeout.rs:196-206 ReceiveTimeout::init inits inner first, then schedules its timer unless terminal","crates/behavior/src/supervising.rs:641-656 Supervising::init inits inner, then extends creates with the configured fleet and observe sends; stashing.rs:95-97 and shutdown.rs:137-139 forward inner init unchanged","crates/behavior-testkit/tests/init_contract.rs:1-8 header documents the double-init/step-before-init findings as direct-misuse boundaries unreachable through the driver"],"validation":["cargo nextest run -p bombay-behavior-testkit --test init_contract -> 6 tests run: 6 passed (at_double_init_duplicates_the_schedule_send, watch_double_init_duplicates_the_observe_send, supervising_double_init_duplicates_the_configured_fleet, full_stack_double_init_duplicates_every_init_effect, proxy_step_before_init_is_inert_until_worker_birth, supervisor_step_before_init_routes_to_unborn_proxies)","cargo nextest run -p bombay-behavior -> 31 passed including driver_interprets_initial_effect_before_receiving and shutdown_composition_preserves_inner_initialization_effects","cargo nextest run -p bombay-behavior-testkit --test boundaries -> 14 tests run: 14 passed including proxy_double_init_is_rejected (should_panic: a proxy initializes once)"]},
    {"id":"CORE-ERRORS","category":"core","subject":"typed errors and termination","question":"Prove errors, phases, Never, Step, and Exit preserve illegal-state constraints.","status":"resolved","decision":"Retain as-is, evidence verified: law-classified (Never uninhabited, verdict.rs:8-10 'the law is the type, not a convention') with Bombay policy Crash classification — Acted is Result<Actions,E>, Step<Ph,Exit<A>> is the become verdict, Step<Never> has no constructible Goto, and NoBirths::Child = Never proves a layer creates nothing.","evidence":["crates/behavior/src/verdict.rs:11 pub enum Never {} — uninhabited; Step<Never> cannot Goto and NoBirths::Child = Never (behavior.rs:291-293)","crates/behavior/src/verdict.rs:16-25 pub enum Step<Ph, R> { Continue, Goto(Ph), Stop(R) } — the Agha 1986 become verdict","crates/behavior/src/exit.rs:12 pub enum Exit<A: Address> { Normal, Collected, LinkDied(A), SupervisionFailed(..) } plus Crash (exit.rs:65) for abnormal termination","crates/behavior/src/behavior.rs:352 Acted<A, Ph, Sends, Birth, E> = Result<Actions<...>, E> — controlled failure is a typed associated Error, never a panic channel","crates/behavior/src/behavior.rs:574-575 and 589-590 run() exhausts Step::Goto(never) via match never {} — unreachable by type, not by convention","crates/behavior-testkit/tests/error_paths.rs:1-6 documents the Fsm mid-drain error asymmetry (drops unprocessed batch) vs the Stop path (preserves it) as a pinned finding"],"validation":["cargo nextest run -p bombay-behavior-testkit --test error_paths -> 9 tests run: 9 passed (fsm_error_mid_drain_drops_the_unprocessed_batch, supervision_propagates_inner_errors_without_touching_slots, at_reaction_error_consumes_the_timer, watch_reaction_error_propagates, driver_propagates_errors_and_preserves_the_tail, ...)","cargo nextest run -p bombay-behavior -> 31 passed including birth_modes_are_disjoint_and_wrappers_forward_them which pins NoBirths/Births disjointness at the type level","cargo nextest run -p bombay-behavior-testkit --test boundaries -> 14 passed including supervision failure exit and unknown-nonce panic guard tests"]},
    {"id":"CORE-SENDS","category":"core","subject":"typed send algebra","question":"Prove exact lane/product selection without erasure or duplicate algebra.","status":"resolved","decision":"Retain as-is, evidence verified: Bombay derivation — Delivery/Recipient/Route form the typed delivery lane, SendProduct<L,R> is the concrete nested product with per-lane SendAlgebra, ServiceSends<M> is the disjoint local-request lane; SendAlgebra is a proven monoid (identity + associativity) at Vec and SendProduct levels; no erased or duplicate send representation exists.","evidence":["crates/behavior/src/behavior.rs:36-106 Route (Global/Child), Recipient (route + PhantomData<fn(M)> message coupling), Delivery { to: Recipient<A,M>, message: M } — message type statically coupled to recipient","crates/behavior/src/behavior.rs:112-116 SendProduct<L,R> { inner: L, own: R } — concrete nested product, one definition reused by every wrapper","crates/behavior/src/behavior.rs:118-139 SendAlgebra monoid with Vec<T> and SendProduct<L,R> impls; append recurses per lane preserving lane-local order","crates/behavior/src/behavior.rs:154-232 ServiceSends<M> — address-less local requests, documented as disjoint from Delivery so interpreters route them with disjoint static implementations","crates/behavior-testkit/tests/driver_accumulation.rs:143-194 proptest send_algebra_is_a_monoid verifies empty is a two-sided identity and append is associative at both Vec and SendProduct levels"],"validation":["cargo nextest run -p bombay-behavior-testkit --test driver_accumulation -> 6 tests run: 6 passed including send_algebra_is_a_monoid (128-case proptest) and driver_accumulates_supervising_send_products_losslessly","cargo nextest run -p bombay-behavior -> 31 passed including ordinary_and_service_send_algebras_have_disjoint_static_dispatch","rg -n '\\bAny\\b|\\bdyn\\s' crates/behavior/src -> 0 matches, so lane selection cannot rely on erasure"]},
    {"id":"MODULE-CORE","category":"modules","subject":"core algebra layer","question":"Verify behavior, verdict, and exit own only core algebra responsibilities.","status":"resolved","decision":"Retain as-is, evidence verified: module ownership is Bombay policy — behavior.rs (aliased as mod algebra via #[path]) owns the fold, effect algebra, send algebra, and driver; verdict.rs owns Never/Step only; exit.rs owns Exit/Crash/RestartDenial/SupervisionFailureReason vocabulary only.","evidence":["crates/behavior/src/lib.rs:9-10 #[path = \"behavior.rs\"] mod algebra; — the core algebra module alias, re-exported as the Acted/Actions/Behavior/State surface at lib.rs:19-23","crates/behavior/src/verdict.rs:1-25 contains only Never and Step — no fold, wrapper, or event lane","crates/behavior/src/exit.rs:1-84 contains only Exit, SupervisionFailureReason, RestartDenial, Crash — termination vocabulary, no fold logic","rg 'impl.*Behavior for|fn init|fn step' over crates/behavior/src/{protocol,verdict,exit}.rs -> 0 matches: no core module leaks transformation responsibilities"],"validation":["rg -n 'impl.*Behavior for|fn init|fn step' crates/behavior/src/protocol.rs crates/behavior/src/verdict.rs crates/behavior/src/exit.rs -> no matches observed","cargo nextest run -p bombay-behavior -> 31 tests run: 31 passed, core algebra suite green","cargo nextest run -p bombay-behavior-testkit --test driver_accumulation -> 6 passed, driver accumulation over the core types green"]},
    {"id":"MODULE-PROTOCOL","category":"modules","subject":"neutral protocol vocabulary","question":"Verify protocol owns values/capabilities but no fold or wrapper.","status":"resolved","decision":"Retain as-is, evidence verified: Bombay policy — protocol.rs is the neutral vocabulary of interpreter-originated lane values (TimerElapsed, PeerStopped, ChildStopped, WorkerStopped, ShutdownRequested, Schedule*) plus extraction-capability traits (TimeEvent, PeerEvent, ChildEvent, WorkerEvent, ShutdownEvent); it defines no fold and no wrapper.","evidence":["crates/behavior/src/protocol.rs:1-5 module doc: 'Neutral typed vocabulary for interpreter-originated event and service lanes ... avoids dependencies between otherwise independent transformations'","crates/behavior/src/protocol.rs contains TimerId/TimerGeneration/ScheduleAt/ScheduleAfter/TimerElapsed/ObservePeer/PeerStopped/ChildStopped/ObserveChild/ReportWorkerStopped/WorkerStopped/ShutdownRequested and the *Event extraction traits — values and capabilities only","rg 'impl.*Behavior for|fn init\\(|fn step\\(' crates/behavior/src/protocol.rs -> 0 matches: no fold or wrapper ownership","crates/behavior/src/behavior.rs:10-13 the core imports protocol values (TimerElapsed, PeerStopped, ...) — dependency points from core/wrappers into protocol, never the reverse"],"validation":["rg -n 'impl.*Behavior for|fn init|fn step' crates/behavior/src/protocol.rs crates/behavior/src/verdict.rs crates/behavior/src/exit.rs -> no matches observed","cargo nextest run -p bombay-behavior -> 31 tests run: 31 passed, all wrappers consume protocol vocabulary correctly","cargo nextest run -p bombay-behavior-testkit --test boundaries -> 14 passed including nested_at_identical_schedules_are_distinguished_by_identity which exercises TimerId/TimerGeneration vocabulary"]},
    {"id":"MODULE-TRANSFORMS","category":"modules","subject":"concrete transformations","question":"Verify wrappers own concrete event sums and truthful forwarding.","status":"resolved","decision":"Retain as-is, evidence verified: Bombay derivation — each wrapper module owns its concrete closed event sum (AtEvent, ReceiveTimeoutEvent, WatchEvent, SupervisionEvent, ShutdownProtocol) and forwards inner lanes unmodified through wrap()/SendProduct.inner; no wrapper reinterprets or relabels an inner lane.","evidence":["crates/behavior/src/deadlined.rs:17 pub enum AtEvent<E>, receive_timeout.rs:17 ReceiveTimeoutEvent<E>, watching.rs:14 WatchEvent<E,A>, supervising.rs:125 SupervisionEvent<E,A>, shutdown.rs:17 ShutdownProtocol<E> — concrete sums owned by their wrapper modules","crates/behavior/src/watching.rs:158-186 Watching::wrap places actions.sends unchanged into SendProduct { inner: actions.sends, own } — truthful forwarding","crates/behavior/src/stashing.rs:95-128 Stashing forwards inner init/step results unchanged except held-message bookkeeping; shutdown.rs:137-151 forwards inner actions verbatim","rg 'enum (AtEvent|WatchEvent|SupervisionEvent|ReceiveTimeoutEvent|ShutdownProtocol|StashRoute)' crates/behavior/src -> each defined exactly once in its owning wrapper module, none in core or protocol"],"validation":["cargo nextest run -p bombay-behavior -> 31 passed including outer_combinators_preserve_the_shutdown_lane, nested_at_composition_routes_stale_and_matching_events, watching_registers_and_reacts_through_messages","cargo nextest run -p bombay-behavior-testkit --test boundaries -> 14 passed and --test driver_accumulation -> 6 passed including driver_accumulates_supervising_send_products_losslessly (lane-order preservation through the full product)","rg -n 'enum (AtEvent|WatchEvent|SupervisionEvent|ReceiveTimeoutEvent|ShutdownProtocol|StashRoute)' crates/behavior/src/*.rs -> 6 matches in 6 files, one sum per wrapper module"]},
    {"id":"STATIC-01","category":"surface","subject":"zero dynamic escape hatches","question":"Prove dyn/Any/TypeId/unsafe/registry/reflection remain absent.","status":"resolved","decision":"Retain as-is, evidence verified: static-dispatch law (AGENTS.md zero-tolerance) — dyn/Any/TypeId/unsafe are absent from every crates/behavior/src file; no registry, Box, or reflection mechanism exists; the checker enforces the same patterns with hard failure.","evidence":["rg -n '\\bdyn\\s+[A-Za-z_]|\\bAny\\b|\\bTypeId\\b|\\bunsafe\\b' crates/behavior/src -> 0 matches across all 12 source files","rg -n 'Box|registry|Registry' crates/behavior/src -> 0 matches — no heap-erased or lookup-based composition","crates/behavior/src/behavior.rs:285-307 BirthMode/NoBirths/Births encode capability at the type level with PhantomData<fn() -> C>, replacing any runtime registry","research/architecture-critical-review-loop/check.sh zero-tolerance block greps the same four patterns over crates/behavior/src and fails on any hit"],"validation":["bash: rg -n '\\bdyn\\s+[A-Za-z_]|\\bAny\\b|\\bTypeId\\b|\\bunsafe\\b' crates/behavior/src | wc -l -> 0","bash: rg -n 'Box|registry|Registry' crates/behavior/src | wc -l -> 0","cargo nextest run -p bombay-behavior -> 31 tests run: 31 passed with fully static dispatch compiled in"]},
    {"id":"REJECT-ERASURE","category":"rejected","subject":"trait objects and erased futures","question":"Confirm rejection and test whether any private escape exists.","status":"resolved","decision":"Rejection upheld, evidence verified: static-dispatch law — no trait object, erased future, or private escape hatch exists anywhere in crates/behavior/src, public or private; the compile_fail doctest on Behavior proves even the named effect-escape seat cannot be introduced.","evidence":["rg -n '\\bdyn\\s+[A-Za-z_]|\\bAny\\b|\\bTypeId\\b' crates/behavior/src -> 0 matches, so no public or private trait-object/erasure escape exists","crates/behavior/src/behavior.rs:412-418 compile_fail doctest: B::Effect associated type does not exist — an erased effect seat fails to compile","crates/behavior/src/behavior.rs:428-430 init/step return impl Future<Output = BehaviorActed<Self>> + Send — RPITIT static futures, not boxed erased futures","crates/behavior/src/lib.rs:6-7 extern crate self as behavior exists only for workers! macro hygiene, not an escape hatch"],"validation":["cargo test -p bombay-behavior --doc -> 1 passed: behavior.rs - algebra::Behavior (line 412) - compile fail ... ok","bash: rg -n '\\bdyn\\s+[A-Za-z_]|\\bAny\\b|\\bTypeId\\b|\\bunsafe\\b' crates/behavior/src | wc -l -> 0","cargo nextest run -p bombay-behavior -> 31 tests run: 31 passed, no erased path exercised or required"]},
    {"id":"REJECT-ENVELOPE","category":"rejected","subject":"global event/effect envelope","question":"Confirm rejection and absence of catch-all internal protocols.","status":"resolved","decision":"Rejection upheld, evidence verified: actor-algebra law — every event protocol is a concrete closed sum per wrapper (AtEvent/WatchEvent/SupervisionEvent/ReceiveTimeoutEvent/ShutdownProtocol) and every effect flows through the typed Actions fields; no universal envelope, catch-all variant, or untyped byte/string protocol exists.","evidence":["rg 'enum (AtEvent|WatchEvent|SupervisionEvent|ReceiveTimeoutEvent|ShutdownProtocol|StashRoute)' crates/behavior/src -> 6 concrete sums in 6 wrapper files, each with named typed variants only","crates/behavior/src/behavior.rs:320 Actions carries sends: Sends, creates: Vec<Create<...>>, become_: Become<A,Ph> — three typed fields, no envelope payload slot","rg -n '\\bAny\\b|serde|Serialize|Deserialize' crates/behavior/src -> 0 matches for Any/serialization-based internal protocol (Any count 0 verified)","crates/behavior/src/protocol.rs owns only typed lane values (TimerElapsed, PeerStopped, ChildStopped, WorkerStopped, ShutdownRequested) — no catch-all message type"],"validation":["bash: rg -n '\\bAny\\b' crates/behavior/src | wc -l -> 0","cargo nextest run -p bombay-behavior -> 31 passed including workers_macro_hides_a_heterogeneous_child_sum (heterogeneity via concrete sums, not envelopes)","cargo nextest run -p bombay-behavior-testkit --test driver_accumulation --test boundaries -> 20 tests run: 20 passed, all lanes observed through concrete typed fields"]}
  ]
}
