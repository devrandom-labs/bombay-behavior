[package]
name = "behaviorpass-perf"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# The FROZEN metric harness — the number the concision loop MAXIMIZES:
# SCORE = k / code-only-LOC of the SUT capability machinery. Higher score =
# fewer lines. It reads the SUT source tree, never its runtime behavior, so a
# novel-but-terse mechanism is measured on equal terms with the baseline. The
# loop must not edit this crate (checks.sh reverts it).
[[bin]]
name = "behaviorpass-perf"
path = "src/main.rs"

[dependencies]

[lints]
workspace = true
