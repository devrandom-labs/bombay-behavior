[package]
name = "behaviorpass-reference"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# The FROZEN gold model of ADR-0030's Behavior algebra: the ~50-line synchronous
# essence-fold plus one model layer per capability. Trace equality to THIS
# defines "working" at every lattice point; divergence is a bug by definition.
# The concision loop must NOT edit this crate (checks.sh reverts it).
[dependencies]
bombay = { workspace = true }

[lints]
workspace = true
